// expected: diagnostic
// expected-code: CST2001

trait Writer {
  write(const char* data) :: void;
}

struct PartialWriter with Writer {
  constructor() {
    ret;
  }
}

main() :: int32 {
  PartialWriter writer = PartialWriter();
  dynamic Writer& erased = dynamic ref writer as Writer;
  ret 0;
}
