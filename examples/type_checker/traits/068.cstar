// expected: diagnostic
// expected-code: CST2001

trait Measurable {
  value() :: int32;
}

struct Meter with Measurable {
  int32 reading;

  constructor(int32 value) {
    self.reading = value;
  }
}

main() :: int32 {
  Meter meter = Meter(1);
  ret 0;
}
