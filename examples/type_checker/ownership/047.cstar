// expected: diagnostic
// expected-code: CST2105

forward(nomove int32^ pointer) :: int32^ {
  ret move pointer;
}

main() :: int32 {
  int32 value = 1;
  int32^ pointer = ref value;
  int32^ moved = forward(move pointer);

  ret deref moved;
}
