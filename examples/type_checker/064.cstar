// expected: fail
// expected-code: CST1001

struct Meter {
  static new(int32 raw) :: Meter {
    Meter meter = Meter(raw);
    ret meter;
  }

  constructor(int32 initial) {
    self.value = initial;
    ret;
  }

  int32 value;
}

main() :: int32 {
  Meter meter = Meter::new(7);
  ret meter.value;
}
