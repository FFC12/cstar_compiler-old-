func() :: void { 
    int x = 20; 
    if(x > 20) {
        int u = 20;
        int z = 30;
        if(u > 30) {
            int p = 40;
        } else {
            int v = 60;
        }
    } else {
        int w = 30;
        int k = 15;
    }   

    ret;
}

main() :: int {
    ret;
}
