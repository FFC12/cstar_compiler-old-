package {
	"std:math"
}

package "std:io" as io

main() :: int32 {
	char* mesg = "Hello world\n";
	int32 x = 10;
	int32 y = x + 5;

	if(x < y) {
		//do something.
		io.print(mesg);
	}

	ret 0;
}

// Let's take a look our features
proto Shape {
	Shape() { // ?constructor
		
	}
}
