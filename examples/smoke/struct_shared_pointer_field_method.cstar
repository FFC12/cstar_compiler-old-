// expected: pass
// expected-exit: 12

struct Counter {
  int32 value;

  constructor(int32 start) {
    self.value = start;
    ret;
  }

  add(int32 amount) {
    self.value += amount;
    ret;
  }
}

main() :: int32 {
  Counter c = Counter(5);
  Counter* sharedValue = ref c;
  Counter* copy = sharedValue;
  sharedValue.value += 2;
  copy.add(5);
  ret sharedValue.value;
}
